# BTC 5M 策略分析紀錄

分析截點：2026-07-16 19:29（台北）。本報告只使用本機紙上交易與 Binance Prediction 公開市場資料，不代表實盤獲利能力。資料庫持續寫入，因此網站上的筆數會比本截面稍多。

## 資料截面

- 37,823 筆一秒觀察、137 個市場、466 筆模擬交易，其中 461 筆已平倉。
- 已平倉策略帳本損益合計約 `-670.56 USDT`。這是多條獨立策略重複下注後的加總，不能視為分散投資組合。
- 目前只有約 20～21 個市場直接保存 Binance Prediction 的正式 `endPrice`；較早資料仍混有現貨 proxy 結算。
- 21 個同時有 proxy 與正式結果的市場中，有 1 個方向不同。另可用下一個連續 5 分鐘市場的 `startPrice` 重建前一局方向；115 局可重建，且在 20 個可交叉驗證市場中 20/20 相符，但這仍是 `next-strike reconstructed`，不能標成直接正式結算。

## 目前已平倉結果

| 策略 | 已平倉 | 實際本金 | 損益 | ROI | 實際成功率 | 損益兩平成功率 |
|---|---:|---:|---:|---:|---:|---:|
| A | 70 | 700.00 | -142.86 | -20.41% | 37.14% | 46.43% |
| B | 48 | 480.00 | -26.83 | -5.59% | 87.50% | 92.57% |
| C | 68 | 680.00 | -172.69 | -25.40% | 36.76% | 48.95% |
| D | 104 | 1,040.00 | -155.54 | -14.96% | 配對 53.85% | 約 69.49% |
| E | 51 | 510.00 | -14.34 | -2.81% | 80.39% | 83.13% |
| E2 | 3 | 30.00 | -17.53 | -58.43% | 33.33% | 樣本不足 |
| F | 92 | 460.00 | -53.42 | -11.61% | 配對 44.57% | 約 55.41% |
| G | 2 | 20.00 | -4.23 | -21.14% | 50.00% | 樣本不足 |
| H | 0 | 0.00 | 0.00 | — | — | — |
| I | 11 | 9.67 | -9.86 | -102.00% | 0.00% | 約 2.04%～2.55% |
| J | 12 | 110.26 | -73.27 | -66.45% | 16.67% | 約 49.48% |

「成功」對 A/C/J 代表盤中目標有成交，對 B/E/E2/G/I 代表結算獲勝，對 D/F 則代表第二腿成功配對；不同策略的成功率不能直接互相比較。E2 只有 3 筆、G 只有 2 筆、H 尚未成交，不能據此判斷策略優劣。

## 失敗機制

### A/C：低價不是反轉訊號

當便宜側不高於 0.20 時，原本的 0.20 最小價差條件幾乎沒有增加過濾效果。A 有 25 筆目標成交、44 筆結算失敗；C 有 24 筆目標成交、43 筆結算失敗。小反彈帶來的獲利不足以補償未反彈時接近整筆歸零，J 也出現相同結構：只有 2/12 達標，10 筆結算失敗。

### B/E：勝率高不等於正期望值

上表與本段的 B 樣本都屬於原本持有到結算的 `B_v1`：一次成功平均只賺約 `0.793`，一次失敗平均虧約 `10.019`，因此一次失敗約抵銷 12.64 次成功；87.50% 的實際勝率仍低於約 92.57% 的損益兩平要求。E 比較接近損益兩平，但 80.39% 的實際勝率仍低於約 83.13% 的要求。這說明勝率本身不是目標，應比較「模型勝率－含費可成交成本」。新增止損後的 B 交易會標記為 `B_v2_stop_loss`，不能直接把兩個版本的結果合併解讀。

### 新增實驗 B/B2：固定本金與時間加權本金共用止損

B 保留固定本金作為控制組，仍只在最後 20 秒、領先側賣價 `0.90`～`0.95` 時考慮進場；新版本只增加與 B2 相同的 `0.60` 止損。B2 的差別是僅在剩餘 `20`～`3` 秒開倉，且要求本金隨時間遞增：

```text
progress = clamp((20 - seconds_left) / (20 - 3), 0, 1)
requested_stake = min_stake + (max_stake - min_stake) × progress ^ size_curve
```

預設 `min_stake=5`、`max_stake=20`、`size_curve=2`。因此曲線前段增加較慢，接近 3 秒才明顯放大；實際成交本金仍受最佳賣價可見量限制。這個公式只是要檢驗「越接近結算，方向風險越低」的假設，並不是已證明的勝率模型。

B 與 B2 進場後都不再無條件持有：持倉側可成交最佳買價嚴格低於各筆交易進場時凍結的止損門檻（預設 `0.60`），且最佳買量足以退出全部 shares 時，按觀測到的實際 bid 模擬止損，不能假設成交在門檻 `0.60`；損益會計入進出兩次 taker fee。若第一檔深度不足則保留 OPEN 等待下一次觀察，避免虛構全量成交。系統目前每秒抓一次策略用 REST 訂單簿，所以這只能代表首次觀測到穿越門檻，不能代表毫秒級止損。

止損不天然改善期望值：短暫跌破後回到 1.00 的市場會被提前砍掉。未來應分開比較歷史 `B_v1`、新 `B_v2_stop_loss` 與 B2，至少報告動態實際本金、止損次數、止損後最終勝方、含費 ROI、最大單筆損失與最大回撤；不能只比較三者的勝率。B 與 B2 使用相同止損後，兩者的前向差異主要用來檢驗「固定本金」和「隨剩餘時間放大本金」的效果。

### D/F：第二腿完成前仍是方向單

D 的 56 次配對合計 `+159.54`，47 次逾時合計 `-304.87`；配對平均賺約 `2.85`，逾時平均虧約 `6.49`。F 的 41 次配對合計 `+93.72`，50 次逾時合計 `-142.03`；兩者平均約為 `+2.29/-2.84`。目前配對率都低於各自依實際盈虧推算的損益兩平配對率。第一腿成交後、第二腿成交前仍是一筆方向單，不能先算成套利。

### 重複曝險與成交偏差

127 個已平倉市場中，109 個同時有至少兩種策略；443/461 筆已平倉交易位於多策略市場。A/C 同場 66 次、其中 65 次同方向；D/F 同場 90 次且全部同方向；A/C/D/F 同場 56 次、其中 55 次同方向。每市場總模擬本金中位數為 35 USDT、P95 為 55 USDT、最高 60 USDT。這不表示要立刻加入總風控，但分析時必須把「市場」而非「交易筆數」當作主要獨立樣本，否則會把同一訊號重複計數。

### 結算與成交偏差

正式結果樣本仍少，proxy 與 official 已觀察到 1/21 方向不一致，因此模型校準應只用 direct official 結果；重建結果只能作探索。紙上交易即使檢查最優價深度，仍未完整模擬排隊順位、API 延遲期間的撤單與多檔吃單，實盤結果通常只會更保守。

## 新增實驗 E2

E2 將原本 E 中較值得觀察的區域獨立出來，不覆蓋 E：

- 剩餘時間 60～30 秒。
- 同方向預測價格 0.60～0.79。
- 買賣價差不超過 0.02，最優價數量必須足夠。
- 至少 60 個有效報酬樣本。
- 現貨移動以第一筆觀察至今的完整整合波動及已經過時間標準化，絕對強度至少 0.75。
- 預測市場較高價方向必須和標準化現貨移動同向。
- 兩側訂單簿時差不超過 500 ms，且報價年齡不超過 2 秒。

這是待驗證假設，不是從現有資料得出的獲利保證。

## 新增實驗 G

G 不使用固定價格高低作為優勢，而先估計方向勝率：

```text
q_raw = Phi[ ln(現貨 / 正式 startPrice) / (sigma × sqrt(剩餘秒數 + 延遲)) ]
q = 0.5 + shrinkage × (q_raw - 0.5)
```

再計算每份可成交成本：

```text
effective_cost = 含滑價模擬成交價 + Predict taker fee
conservative_edge = q - 不確定性緩衝 - effective_cost
```

預設只在以下條件進場：

- 剩餘 180～30 秒、價格 0.55～0.85、價差不超過 0.02。
- 至少 60 個有效報酬樣本，訂單簿兩側時間差不超過 500 ms，報價年齡不超過 2 秒。
- 保守淨優勢至少 0.05，而且不得低於三倍當前價差。
- 最優價可見深度足以容納完整模擬本金；設定的 50 bps 滑價會實際提高進場價並進入損益。

每筆 G/E2 交易會保存模型版本、機率、波動、淨優勢及診斷資料，後續應按策略版本及時間順序驗證，不能把同一批資料同時用來選參數和宣稱有效。

## 新增實驗 H：尾盤逆轉狙擊偵測

H 是一個分成「偵測」與「武裝」兩階段的狀態實驗：

1. 偵測階段只計算連續市場。原本領先的一側在最後 5 秒曾達 `0.90`，但正式結算由另一側獲勝，才算一次尾盤逆轉；一般市場或資料不完整的市場會中斷連續次數。
2. 連續兩次逆轉後，H 從下一個市場開始武裝。最後 20 秒內，若唯一一側的最佳賣價不高於 `0.01` 且訂單簿新鮮，便按該價位可見掛單量模擬買入並持有到結算；`10 USDT` 只作為本金上限。
3. H 交易獲勝會把連敗歸零；兩筆 H 交易連續失敗則解除武裝並重新等待兩次連續逆轉。沒有符合 0.01 成交條件的市場不算失敗。

狀態、逐市場偵測事件及尚待正式結果的結算會持久保存。若市場結束當下只有現貨 proxy，H 不用它推進逆轉或連敗狀態，而是等待背景補查正式 `endPrice`，避免把不同價格源的最後一秒差異誤判成逆轉。

這個策略只能檢驗「連續尾盤逆轉後，低價側勝率是否改變」的假設，不能證明有人狙擊或操縱市場。一秒 REST 取樣也可能漏掉秒內變化。現有資料中，最後 20 秒出現 `0.01` 很常見，但多數只是即將歸零的正常輸家；許多價位只有少量掛單，因此每筆會依可見量縮小，而不是假設完整 `10 USDT / 0.01 = 1,000` 份成交。分析時必須同時報告觸發次數、實際成交本金、正式勝率、損益與最大連敗，不能只看少數極端獲利。

## 新增實驗 I：無條件 0.01 對照組

I 是刻意簡單的「賭狗策略」：不等待 H 武裝；在剩餘時間超過 3 秒時，只要唯一一側的最佳賣價不高於 `0.01` 且訂單簿新鮮，就按該價位可見量模擬買入一次並持有到結算。倒數正好 3 秒以及更晚都不進場，`1 USDT` 是本金上限而非最低成交要求。

I 的主要價值是作為 H 的對照組：

- I 測量「任何 0.01 低價側」的基準勝率與期望值。
- H 測量「連續兩次尾盤逆轉後的 0.01 低價側」。
- 只有 H 在足夠的未來樣本中明顯優於 I，才能支持狀態訊號可能有額外資訊；兩者都虧損則代表 `0.01` 多半只是正常輸家定價。

在進場價恰為 `0.01`、Predict taker 費率為 200 bps 且不計滑價時，I 的損益兩平勝率約為 `1.02%`。單次中獎金額很大、但大量小額歸零會造成極端偏態，因此應同時查看市場數、成交深度、連敗長度及正式結算，而不能只看一次罕見獲勝。

## 新增實驗 J：尾盤低價反彈

J 沿用 C 的「買便宜側、等待價格反彈到實際進場價兩倍」概念，但把訊號移到市場最後兩分鐘：

- 只在 `3 < seconds_left <= 120` 時允許開倉，最後 3 秒不追價。
- UP／DOWN 價差至少 `0.20`，便宜側最佳賣價不高於 `0.20`。
- 報價必須新鮮且未交叉；符合價位的正數可見掛單有多少就成交多少，`10 USDT` 為本金上限。
- 目標價為實際買價的 `2` 倍；例如 `0.15` 買入、最佳買價到 `0.30` 時模擬出場。到結算前沒有達標則依方向結算。

J 與 C 的差別除了市場階段，也包含部分成交記帳，因此應同時比較每筆實際本金，不能只比較交易數與總損益。它也和 I 不同：I 在 `0.01` 後直接等待罕見結算翻盤；J 的主要收益來源是假設尾盤仍會出現短暫價格反彈。最後兩分鐘的時間較短，未達標部位更容易直接歸零，因此仍需按市場順序累積足夠樣本，不能用少數成功反彈宣稱有效。

## 新增實驗 K：基差校正終局勝率

K 回應「不要用預測盤價格判斷方向」的需求。Binance 現貨和 Prediction 的正式結算價可能來自不同報價源，因此不能直接把目前 Binance 價格與官方 `startPrice` 相減。令：

```text
S0 = 正式 startPrice
B0 = 同市場最早、剩餘至少 290 秒的 Binance spot
Bt = 目前 Binance spot
adjusted_spot = Bt × S0 / B0
distance = ln(adjusted_spot / S0) = ln(Bt / B0)
```

這會先消除開盤時的固定報價源基差。再把距離轉為剩餘時間內守住目前方向的機率：

```text
variance = sigma² × (seconds_left + latency)
           + (basis_uncertainty_bps / 10000)²
z = abs(distance) / sqrt(variance)
p_raw = Phi(z)
p_calibrated = sigmoid(calibration_slope × logit(p_raw))
```

- `distance > 0` 只考慮 UP，`distance < 0` 只考慮 DOWN；不能因為另一側較便宜就反轉模型方向。
- 預設進場觀察窗為剩餘 180～20 秒，至少 60 個有效報酬樣本、波動率下限 `0.00002`、基差不確定性 `1.5 bps`、延遲假設 2 秒、校準斜率 `0.5`、最低模型勝率 `0.65`。
- K 沒有固定最低／最高市場價格。訂單簿不參與方向或勝率，只用於確認報價未交叉、價差與時間品質合格、最優價深度足夠，以及計入 50 bps 滑價和價格敏感 taker fee。
- 只有 `p_calibrated - effective_cost` 同時不低於 `0.05` 和三倍買賣價差才模擬成交。即使模型認為勝率 80%，若含費成本是 0.90 仍應拒絕。
- 波動樣本查詢限制在訊號時間以前，避免離線回放不小心使用未來價格。

K 與 E2 都使用同源 Binance 價格移動，但 E2 用已經經過的時間衡量「過去動能」，K 用剩餘時間估計「終局勝率」。G 則直接比較 Binance spot 與官方 `startPrice`，可能混入開盤報價源基差。

### K 的探索性回放限制

一個固定倒數約 120 秒、距離強度至少 `2σ` 的替代版本，在嚴格按時間切分的探索性回放中得到 19/19、約 `+16.98 USDT`；但其中 14 筆使用 next-strike 重建標籤，direct-official holdout 只有 5 筆，且回放對排隊、延遲、滑價與深度衰減仍過度樂觀。19/19 的 95% Wilson 勝率下界只有約 83.2%，低於該批中位進場價約 0.92 所需的損益兩平勝率。因此這只能支持把「距離／剩餘波動」列入 forward paper test，不能宣稱已找到獲利策略；目前 K 採用較可解釋且可調校的終局機率版本，而不是照探索結果硬編一組最佳參數。

## 新增實驗 L：雙向前段反彈

L 要測試的是開盤後一分鐘內的雙向擺動，而不是預測最後勝方。UP、DOWN 兩側各自等待賣價不高於 `0.50`，每腿最多使用總本金的一半；兩腿可在不同秒成交，一側沒有達到門檻不會阻擋另一側，也不會在前 60 秒結束後補買。正常二元簿的兩側賣價合計通常不低於 1，因此兩側同一秒都低於 0.50 很少見；這個策略主要會在價格前後擺動時分時建立兩腿。

進場只承接新鮮、未交叉訂單簿的最佳賣價可見量，因此每腿可能只部分成交。任一腿的最佳買價達到凍結目標（預設 `0.70`），且同一價位的可見買量足以退出整腿時，才以 `0.70` 記錄保守模擬成交並計算進出雙邊費用；深度不足會保持 OPEN，未出場腿則交由正式結算。

這不是配對套利：

- 相同 USDT 在不同進場價會得到不同 shares，終局曝險不對稱。
- 若兩腿都在 0.50 買入、都沒有觸及 0.70，最後一勝一負的毛收益只是打平本金，扣費後必然為負。
- 若只有一腿到 0.70 後賣出、另一腿最後歸零，整輪仍可能虧損。
- 若只有一側在前 60 秒符合條件，該輪會保留單方向曝險。

網站摘要目前把 UP／DOWN 各腿分別算一筆交易；要判定策略是否有效，離線分析還應按 `market_id` 合併兩腿，報告雙腿完成率、單腿／雙腿達標率、進場價合計、每輪含費 PnL、未配成雙腿的比例與最大回撤。若在一輪只開第一腿後按測量歸零，稍後第二腿可能進入新 cohort，因此最好只在兩輪之間重設 L。

## 新增實驗 M：開盤方向盲單

M 是最簡單的開盤方向對照組。每輪開盤後前 `10` 秒內，第一個訊號和實際成交條件同時有效的每秒快照計算：

```text
distance = Binance BTCUSDT spot - Prediction official startPrice
distance > 0  -> BUY UP
distance < 0  -> BUY DOWN
distance = 0  -> 等下一筆
```

方向一旦由正負號決定，就不會因另一側 Prediction token 較便宜而翻向。選定側的 Prediction ask、bid、新鮮度與可見 ask size 只負責模擬成交；沒有最低或最高買價，也沒有盤中停利／停損。單筆本金上限預設 `10 USDT`，最佳賣價有多少可見量就成交多少，剩餘本金不補單，之後持有到正式結算並只計入進場 taker fee。

這個訊號有意保持簡單，但有三個重要限制：

- `startPrice` 是正式 Prediction 結算來源的起始價，spot 是 Binance BTCUSDT；跨來源固定基差可能讓差值在開盤長期偏正或偏負。
- 第一筆是 REST 每秒輪詢首次取得的可成交快照，不是開盤毫秒瞬間；實際進場可能延遲數秒。
- 沒有價格上限代表即使方向訊號很弱、選定 token ask 已接近 1，也仍會買入；高表面勝率不等於正期望值。

因此 M 的用途是回答「開盤當下的價格方向符號本身有沒有預測力」，而不是直接宣稱策略可獲利。評估時至少要按 `distance_bps`、實際進場秒數、UP／DOWN 比例、進場價、fill ratio 分組，並同時報告正式勝率、含費 ROI、損益兩平勝率與方向偏態；若方向幾乎固定在一側，應先檢查跨來源基差，而不是解讀成市場訊號。

## 後續改進與判定標準

1. 以期望值而非表面勝率排序：每個版本同時報告模型勝率、含費成交成本、ROI、最大連敗與損益兩平勝率。
2. 將 `direct_official`、`spot_proxy`、`next-strike reconstructed` 分開；核心結論只用 direct official。K 至少累積 100～300 筆 direct-official 結果後才重新選參數。
3. 依時間順序切 development／validation／holdout，參數一旦選定就凍結；網站調整參數後按 `strategy_version + config snapshot` 分組，不能混成同一策略。
4. 除了成交交易，建議在固定倒數 180／120／60／30 秒保存 K 的未成交預測，計算 Brier score、log loss 與分箱校準。只看通過成本門檻的成交樣本會產生選擇偏誤。
5. 報告以「獨立市場數」為主，另列同市場、同方向的重複曝險；這是統計修正，不是本輪新增總資金或停損限制。
6. 紙上執行繼續加入報價年齡、兩側時差、可見深度、費用與滑價，並將無法完整成交視為拒絕，而不是假設理想成交。

## 本輪刻意不加入的項目

依照資料蒐集需求，本輪不加入跨策略總資金、每日停損或每市場合併曝險控制。A～M（包含 B2、E2）保持獨立帳本，以便比較各自訊號；因此網站合計損益不能視為已分散的投資組合結果。
