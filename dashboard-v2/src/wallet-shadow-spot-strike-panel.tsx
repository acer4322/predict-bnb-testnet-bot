// RETIRED 2026-08-14: historical Spot/Strike T-10s results remain in SQLite,
// but this cohort no longer produces forward decisions or events. Keep this
// compatibility stub because wallet-shadow-page still imports it.
export default function WalletShadowSpotStrikePanel() {
  return null
}
