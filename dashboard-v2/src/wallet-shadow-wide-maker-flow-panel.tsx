// RETIRED 2026-08-14: historical SQLite data is preserved, but the Wide Maker
// + Maker Flow Alpha + Tail Insurance cohorts no longer produce forward data.
// Keep a compatibility stub because wallet-shadow-page still imports it.
export default function WalletShadowWideMakerFlowPanel() {
  return null
}
